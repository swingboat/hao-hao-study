/**
 * @hao/ui — 学生端 / 运营端共享的 React 组件层（基于 shadcn/ui + Tailwind 4）
 *
 * 设计原则：
 *   - 仅放真正双端共用的组件（按钮、卡片、对话框、表单原子）
 *   - 端特异组件留在 apps/web/components 或 apps/admin/components
 *
 * 当前为骨架，组件随 M6/M7 阶段按需迁移。
 */
export const UI_VERSION = '0.1.0-skeleton';
